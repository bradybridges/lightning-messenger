import React from 'react';
import { shallow } from 'enzyme';
import Home from './Home';

describe('Home', () => {
  let wrapper;
  const mockNavigate = { navigate: jest.fn() };
  // beforeEach(() => {
  //   wrapper = shallow(<Home navigation={mockNavigate} />);
  // });

  it('should match snapshot', () => {

  });
});